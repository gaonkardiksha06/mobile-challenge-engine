# Challenge: JWT Authentication

**Course:** Backend Basics

## Goal

Complete all tasks for **JWT Authentication** in the course project. Work on this challenge only until you pass review (≥ 80%).

## Concepts

- bcrypt
- JWT
- Protected routes

## Tasks

1. Register API with hashed password
2. Login API with token generation
3. Protected profile route

## Technical Requirements

- Implement features in the files listed in `metadata.json` (`filesToCheck`).
- Use TypeScript/JavaScript patterns appropriate for Node.js.
- No `console.log` in production paths; pass ESLint where configured.
- Add `testID` props on key interactive elements for automated tests (e.g. `testID="follow-button"`).

## Verify

```bash
cd courses/04-backend-basics/project
npm run review -- --challenge=04-jwt-authentication
```

Pass threshold: **≥ 80%**.
