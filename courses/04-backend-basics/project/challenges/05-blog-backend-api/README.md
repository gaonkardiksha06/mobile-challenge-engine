# Challenge: Blog Backend API (Capstone)

**Course:** Backend Basics

## Goal

Complete all tasks for **Blog Backend API (Capstone)** in the course project. Work on this challenge only until you pass review (≥ 80%).

## Concepts

- User auth
- CRUD posts
- JWT protection
- MongoDB

## Tasks

1. User registration and login
2. CRUD for blog posts
3. JWT-protected routes
4. MongoDB storage

## Technical Requirements

- Implement features in the files listed in `metadata.json` (`filesToCheck`).
- Use TypeScript/JavaScript patterns appropriate for Node.js.
- No `console.log` in production paths; pass ESLint where configured.
- Add `testID` props on key interactive elements for automated tests (e.g. `testID="follow-button"`).

## Verify

```bash
cd courses/04-backend-basics/project
npm run review -- --challenge=05-blog-backend-api
```

Pass threshold: **≥ 80%**.
