# Challenge: Payments & Optimization

**Course:** Advanced React Native

## Goal

Complete all tasks for **Payments & Optimization** in the course project. Work on this challenge only until you pass review (≥ 80%).

## Concepts

- Stripe
- Memoization
- Lazy loading
- Performance

## Tasks

1. Payment/checkout screen and success page
2. Memoization, lazy loading, reduce re-renders

## Technical Requirements

- Implement features in the files listed in `metadata.json` (`filesToCheck`).
- Use TypeScript/JavaScript patterns appropriate for React Native / Expo.
- No `console.log` in production paths; pass ESLint where configured.
- Add `testID` props on key interactive elements for automated tests (e.g. `testID="follow-button"`).

## Verify

```bash
cd courses/05-react-native-advanced/project
npm run review -- --challenge=05-payments-optimization
```

Pass threshold: **≥ 80%**.
