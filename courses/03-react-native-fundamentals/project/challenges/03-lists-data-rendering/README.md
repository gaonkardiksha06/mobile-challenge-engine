# Challenge: Lists & Data Rendering

**Course:** React Native Fundamentals

## Goal

Complete all tasks for **Lists & Data Rendering** in the course project. Work on this challenge only until you pass review (≥ 80%).

## Concepts

- FlatList
- ScrollView
- map()
- Search filter
- Empty states
- Loading

## Tasks

1. Vertical image gallery + horizontal category scroll (ScrollView)
2. FlatList feed with posts, usernames, likes
3. Search bar with live filter
4. Empty state ("No Data") and loading spinner

## Technical Requirements

- Implement features in the files listed in `metadata.json` (`filesToCheck`).
- Use TypeScript/JavaScript patterns appropriate for React Native / Expo.
- No `console.log` in production paths; pass ESLint where configured.
- Add `testID` props on key interactive elements for automated tests (e.g. `testID="follow-button"`).

## Verify

```bash
cd courses/03-react-native-fundamentals/project
npm run review -- --challenge=03-lists-data-rendering
```

Pass threshold: **≥ 80%**.
