# Challenge: Node.js Basics

**Course:** Backend Basics

## Goal

Complete all tasks for **Node.js Basics** in the course project. Work on this challenge only until you pass review (≥ 80%).

## Concepts

- Modules
- npm
- File system
- HTTP server

## Tasks

1. Setup Node project and simple HTTP server
2. Read and write files
3. Custom modules with export/import

## Technical Requirements

- Implement features in the files listed in `metadata.json` (`filesToCheck`).
- Use TypeScript/JavaScript patterns appropriate for Node.js.
- No `console.log` in production paths; pass ESLint where configured.
- Add `testID` props on key interactive elements for automated tests (e.g. `testID="follow-button"`).

## Verify

```bash
cd courses/02-backend-basics/project
npm run review -- --challenge=01-nodejs-basics
```

Pass threshold: **≥ 80%**.
