# Challenge: Navigation System

**Course:** React Native Fundamentals

## Goal

Complete all tasks for **Navigation System** in the course project. Work on this challenge only until you pass review (≥ 80%).

## Concepts

- React Navigation
- Stack navigation
- Tabs
- Route params
- Dynamic headers

## Tasks

1. Install navigation and create Home + Profile screens
2. Stack: Home → Details with username param
3. Bottom tabs: Home, Search, Profile
4. Dynamic header title and custom header styling

## Technical Requirements

- Implement features in the files listed in `metadata.json` (`filesToCheck`).
- Use TypeScript/JavaScript patterns appropriate for React Native / Expo.
- No `console.log` in production paths; pass ESLint where configured.
- Add `testID` props on key interactive elements for automated tests (e.g. `testID="follow-button"`).

## Verify

```bash
cd courses/01-react-native-fundamentals/project
npm run review -- --challenge=02-navigation-system
```

Pass threshold: **≥ 80%**.
