# Challenge: API Integration

**Course:** React Native Fundamentals

## Goal

Complete all tasks for **API Integration** in the course project. Work on this challenge only until you pass review (≥ 80%).

## Concepts

- fetch
- async/await
- useEffect
- Loading states
- Error handling

## Tasks

1. Fetch users from JSONPlaceholder (cards with email, avatar)
2. Weather app: search city, temperature, weather icon
3. Network error UI with retry button

## Technical Requirements

- Implement features in the files listed in `metadata.json` (`filesToCheck`).
- Use TypeScript/JavaScript patterns appropriate for React Native / Expo.
- No `console.log` in production paths; pass ESLint where configured.
- Add `testID` props on key interactive elements for automated tests (e.g. `testID="follow-button"`).

## Verify

```bash
cd courses/01-react-native-fundamentals/project
npm run review -- --challenge=05-api-integration
```

Pass threshold: **≥ 80%**.
