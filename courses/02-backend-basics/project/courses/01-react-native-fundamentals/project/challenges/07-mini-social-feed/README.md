# Challenge: Mini Social Feed (Capstone)

**Course:** React Native Fundamentals

## Goal

Complete all tasks for **Mini Social Feed (Capstone)** in the course project. Work on this challenge only until you pass review (≥ 80%).

## Concepts

- Auth UI
- Navigation
- API posts
- AsyncStorage
- FlatList
- Profile

## Tasks

1. Authentication UI (login/signup screens)
2. Tab + stack navigation wired together
3. API-driven post feed with FlatList
4. AsyncStorage login persistence
5. Profile page with user info

## Technical Requirements

- Implement features in the files listed in `metadata.json` (`filesToCheck`).
- Use TypeScript/JavaScript patterns appropriate for React Native / Expo.
- No `console.log` in production paths; pass ESLint where configured.
- Add `testID` props on key interactive elements for automated tests (e.g. `testID="follow-button"`).

## Verify

```bash
cd courses/01-react-native-fundamentals/project
npm run review -- --challenge=07-mini-social-feed
```

Pass threshold: **≥ 80%**.
