# Challenge: Forms & Validation

**Course:** React Native Fundamentals

## Goal

Complete all tasks for **Forms & Validation** in the course project. Work on this challenge only until you pass review (≥ 80%).

## Concepts

- TextInput
- useState
- Form validation
- Multi-step forms

## Tasks

1. Login form: email, password; validate empty fields and password length
2. Signup: confirm password, username, error messages
3. Multi-step form with step indicator and Next/Previous

## Technical Requirements

- Implement features in the files listed in `metadata.json` (`filesToCheck`).
- Use TypeScript/JavaScript patterns appropriate for React Native / Expo.
- No `console.log` in production paths; pass ESLint where configured.
- Add `testID` props on key interactive elements for automated tests (e.g. `testID="follow-button"`).

## Verify

```bash
cd courses/01-react-native-fundamentals/project
npm run review -- --challenge=04-forms-validation
```

Pass threshold: **≥ 80%**.
