# Challenge: Redux Toolkit

**Course:** Advanced React Native

## Goal

Complete all tasks for **Redux Toolkit** in the course project. Work on this challenge only until you pass review (≥ 80%).

## Concepts

- Store
- Slice
- Actions
- createAsyncThunk

## Tasks

1. Counter app (increment/decrement)
2. Todo state (add/delete)
3. Fetch products API into Redux

## Technical Requirements

- Implement features in the files listed in `metadata.json` (`filesToCheck`).
- Use TypeScript/JavaScript patterns appropriate for React Native / Expo.
- No `console.log` in production paths; pass ESLint where configured.
- Add `testID` props on key interactive elements for automated tests (e.g. `testID="follow-button"`).

## Verify

```bash
cd courses/03-react-native-advanced/project
npm run review -- --challenge=01-redux-toolkit
```

Pass threshold: **≥ 80%**.
